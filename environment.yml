name: contribution_search
channels:
  - conda-forge
  - defaults
dependencies:
  - lxml
  - pathlib
  - pandas
  - panel
  - pip
  - pre-commit
  - requests
  - jupyter-panel-proxy
  - pip:
    - python-dotenv
    - pytest
    - pytest-cov
